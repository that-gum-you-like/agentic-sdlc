# Spec: Douglas Documentation

## Acceptance Criteria
- `docs/architecture.md` exists (>100 lines): system diagram, service overview, screen map, data flow
- `docs/api/services.md` exists (>100 lines): top 15 services documented with signatures and return shapes
- `docs/agents/agent-system.md` exists (>100 lines): agent roster, queue lifecycle, memory layers, Matrix rooms
- All counts and references accurate against live codebase

## Implementation Notes
- Douglas subagents use model: haiku (support agent per budget.json)
- Each doc generated by reading actual source files, not from memory
