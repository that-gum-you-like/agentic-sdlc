# Python virtual environment
.venv/

# Environment secrets (never commit actual keys)
.env

# Runtime data (per-project, not framework)
pm/
.paperclip.env

# Ephemeral runtime outputs (regenerated by scripts on every run)
agents/model-intel.json
agents/pattern-hunt-output.json

